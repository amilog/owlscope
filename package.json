{
  "name": "owlscope-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "OwlScope — Universal Debug & Monitoring Tool (monorepo root)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @owlscope/desktop",
    "build:sdk": "npm run build -w owlscope",
    "build": "npm run build -w @owlscope/protocol && npm run build -w owlscope && npm run build -w @owlscope/desktop",
    "clean": "rimraf 'apps/*/out' 'apps/*/dist' 'packages/*/dist'"
  },
  "engines": {
    "node": ">=20"
  }
}
